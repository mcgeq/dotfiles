# edgy.nvim - 智能窗口布局管理

## 📖 简介

edgy.nvim 是一个智能窗口布局管理器，自动将特定类型的窗口（文件树、终端、诊断等）固定在编辑器的边缘（edges），保持主编辑区域整洁。

## 🎯 核心功能

- ✅ 自动识别窗口类型
- ✅ 固定边栏到四个边缘（上/下/左/右）
- ✅ 智能调整窗口大小
- ✅ 支持多种插件（NeoTree、Trouble、Terminal 等）
- ✅ 保持主编辑区域稳定
- ✅ 平滑动画效果

## 🗺️ 布局示意

```
        Top (顶部边栏)
┌─────────────────────────┐
│    Breadcrumbs/Tabs     │
├──────┬──────────┬───────┤
│      │          │       │
│ Left │  Editor  │ Right │
│ Neo  │  (Main)  │ Out   │
│ Tree │  Area    │ line  │
│      │          │       │
├──────┴──────────┴───────┤
│   Bottom (Terminal)     │
└─────────────────────────┘
```

## 📦 默认支持的窗口

| 窗口类型 | 默认位置 | 说明 |
|---------|---------|------|
| **neo-tree** | Left | 文件树 |
| **aerial** | Right | 代码大纲 |
| **symbols-outline** | Right | LSP 符号列表 |
| **trouble** | Bottom | 诊断/错误列表 |
| **toggleterm** | Bottom | 终端 |
| **quickfix** | Bottom | 快速修复列表 |
| **help** | Right | 帮助文档 |
| **man** | Right | Man 手册 |

## 🚀 基本使用

### 自动布局

打开任何支持的窗口，edgy 自动固定到对应边缘：

```vim
" 打开文件树
:Neotree
# ✅ 自动固定到左侧，宽度 25%

" 打开终端
:ToggleTerm
# ✅ 自动固定到底部，高度 30%

" 打开大纲
:AerialToggle
# ✅ 自动固定到右侧，宽度 20%

" 打开诊断
:Trouble
# ✅ 自动固定到底部，高度 25%
```

### 标准开发布局

```vim
<leader>e          # NeoTree (左侧)
:AerialToggle      # 代码大纲 (右侧)
:ToggleTerm        # 终端 (底部)

# 自动结果：
┌────────┬─────────────────┬──────────┐
│        │                 │          │
│ Neo    │   Editor        │ Aerial   │
│ Tree   │                 │ ├─func1  │
│ ├─src  │   function()    │ ├─func2  │
│ │├─app │   {             │ └─func3  │
│ │└─api │     ...         │          │
│        │   }             │          │
├────────┴─────────────────┴──────────┤
│   $ npm run dev                     │
│   > Server running on :3000         │
└─────────────────────────────────────┘
```

## ⌨️ 窗口导航

### 快捷键

```vim
Ctrl-h     " 跳转到左边窗口
Ctrl-l     " 跳转到右边窗口
Ctrl-j     " 跳转到下边窗口
Ctrl-k     " 跳转到上边窗口

Ctrl-w >   " 向右扩展边栏
Ctrl-w <   " 向左缩小边栏
Ctrl-w +   " 向下扩展边栏
Ctrl-w -   " 向上缩小边栏
```

### edgy 专用命令

```vim
:Edgy toggle        " 切换所有边栏
:Edgy toggle left   " 只切换左边栏
:Edgy toggle right  " 只切换右边栏
:Edgy toggle bottom " 只切换底部边栏
:Edgy show          " 显示所有边栏
:Edgy close         " 关闭所有边栏
```

## 💡 使用场景

### 场景 1：前端开发

```vim
" 文件树 + 编辑器 + 组件大纲 + 开发服务器
<leader>e          # NeoTree
:AerialToggle      # React 组件大纲
:ToggleTerm        # npm run dev

┌────────┬──────────────────┬──────────┐
│ 📁    │  App.tsx         │ 📋       │
│ Neo    │                  │ Aerial   │
│ Tree   │  export default  │ ├─App    │
│        │  function App()  │ │ ├─Header│
│ src/   │  {               │ │ └─Footer│
│ ├─App  │    return (      │ └─Props  │
│ └─comp │      <div>       │          │
├────────┴──────────────────┴──────────┤
│ $ npm run dev                        │
│ ✓ Local: http://localhost:5173      │
└──────────────────────────────────────┘
```

### 场景 2：调试模式

```vim
:Trouble           # 错误列表
:ToggleTerm        # 测试输出

┌────────┬──────────────────┐
│ 📁    │  main.go         │
│ Neo    │                  │
│ Tree   │  func main() {   │
│        │    // code       │
│        │  }               │
├────────┴──────────────────┤
│ ⚠️  Trouble (3 errors)    │
│ main.go:10 undefined var │
│ main.go:15 type mismatch │
├───────────────────────────┤
│ $ go test ./...          │
│ FAIL: TestMain           │
└───────────────────────────┘
```

### 场景 3：文档查看

```vim
:help edgy

┌──────────────┬────────────────┐
│              │                │
│   Editor     │   :help edgy   │
│              │                │
│   main.go    │   INTRODUCTION │
│              │   Edgy is a... │
│              │                │
└──────────────┴────────────────┘
```

### 场景 4：多任务并行

```vim
" 同时查看代码、大纲、错误、终端
<leader>e          # 文件树
:AerialToggle      # 大纲
:Trouble           # 错误
:ToggleTerm        # 终端

┌──┬──────────┬──┐
│N │  Editor  │A │
│e │          │e │
│o │          │r │
│T │          │i │
│r │          │a │
│e │          │l │
│e │          │  │
├──┴──────────┴──┤
│   Trouble      │
├────────────────┤
│   Terminal     │
└────────────────┘
```

## 🎨 布局模式

### 1. 专注模式（最小边栏）

```vim
:Edgy close

┌─────────────────────┐
│                     │
│                     │
│      Editor         │
│      (Full)         │
│                     │
│                     │
└─────────────────────┘
```

### 2. 标准模式（左+底）

```vim
<leader>e
:ToggleTerm

┌────────┬────────────┐
│        │            │
│ Neo    │   Editor   │
│ Tree   │            │
│        │            │
├────────┴────────────┤
│    Terminal         │
└─────────────────────┘
```

### 3. 全功能模式（四边）

```vim
<leader>e
:AerialToggle
:Trouble
:ToggleTerm

┌─────────────────────┐
│      Tabline        │
├──┬──────────────┬───┤
│N │   Editor     │ A │
│e │              │ e │
│o │              │ r │
│T │              │ i │
│r │              │ a │
│e │              │ l │
│e │              │   │
├──┴──────────────┴───┤
│     Trouble         │
├─────────────────────┤
│     Terminal        │
└─────────────────────┘
```

## 🔧 自定义配置

### 调整边栏大小

```lua
{
  "folke/edgy.nvim",
  opts = {
    bottom = {
      {
        ft = "toggleterm",
        size = { height = 0.25 },  -- 25% 高度
      },
      {
        ft = "trouble",
        size = { height = 15 },    -- 固定15行
      },
    },
    left = {
      {
        ft = "neo-tree",
        size = { width = 30 },     -- 固定30列
      },
    },
    right = {
      {
        ft = "aerial",
        size = { width = 0.20 },   -- 20% 宽度
      },
    },
  },
}
```

### 动画配置

```lua
opts = {
  animate = {
    enabled = true,
    fps = 60,           -- 每秒60帧
    cps = 200,          -- 每秒200字符
  },
}
```

## 💡 实用技巧

### 1. 创建布局快捷键

```lua
-- 在配置中添加
vim.keymap.set("n", "<leader>ld", function()
  vim.cmd("Neotree")
  vim.cmd("AerialToggle")
  vim.cmd("ToggleTerm")
end, { desc = "开发布局" })
```

### 2. 自动打开布局

```lua
-- 特定文件类型自动打开大纲
vim.api.nvim_create_autocmd("FileType", {
  pattern = { "typescript", "javascript", "rust" },
  callback = function()
    vim.cmd("AerialToggle")
  end,
})
```

### 3. 保存布局状态

edgy 自动记住边栏的打开/关闭状态，下次打开项目时自动恢复。

## 🆚 对比普通分割

| 特性 | 普通分割 | edgy.nvim |
|------|---------|-----------|
| 窗口位置 | 不固定 | 固定边缘 |
| 主编辑区 | 受影响 | 保持稳定 |
| 大小调整 | 影响所有 | 只影响边栏 |
| 布局管理 | 手动 | 自动 |
| 一致性 | 容易乱 | 始终整洁 |

## 📊 工作流示例

### 完整开发会话

```vim
1. 启动项目
   cd ~/project
   nvim

2. 打开文件树
   <leader>e

3. 找到文件并打开
   # 在 NeoTree 中导航
   # 按 <CR> 打开

4. 查看代码结构
   :AerialToggle

5. 运行开发服务器
   :ToggleTerm
   npm run dev

6. 发现错误，打开诊断
   :Trouble

7. 查看 API 文档
   :help some-api

# 所有窗口自动组织
# 编辑区域始终清晰
# 无需手动管理布局
```

## 📚 总结

**edgy.nvim 的价值**：
- 🎯 **自动化** - 无需手动管理窗口位置
- 📐 **一致性** - 布局始终整洁专业
- 🚀 **高效** - 专注主编辑区域
- 🔧 **智能** - 自动识别窗口类型

**最适合**：
- 同时使用多个辅助窗口
- 需要稳定的窗口布局
- 多任务并行开发
- 专业开发环境

让你的 Neovim 拥有 IDE 级别的窗口管理体验！🎉
