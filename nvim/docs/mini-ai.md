# mini-ai - 增强的文本对象

## 📖 简介

`mini-ai` 是一个提供增强文本对象的插件，让你可以更智能地选择和操作代码块。它扩展了 Vim 原生的 `i`（inside）和 `a`（around）文本对象。

## 🎯 核心功能

### 内置文本对象增强

| 文本对象 | 说明 | 示例 |
|---------|------|------|
| `ia` / `aa` | 函数参数 | `cia` 修改函数参数 |
| `ib` / `ab` | 括号块 `()` | `dab` 删除括号及内容 |
| `iB` / `aB` | 大括号块 `{}` | `yaB` 复制大括号及内容 |
| `i[` / `a[` | 方括号 `[]` | `vi[` 选择方括号内容 |
| `iq` / `aq` | 引号（智能识别 `'` `"` `` ` ``） | `ciq` 修改引号内容 |
| `it` / `at` | HTML/XML 标签 | `dit` 删除标签内容 |
| `if` / `af` | 函数调用 | `vaf` 选择整个函数调用 |
| `ik` / `ak` | 键值对 | `cik` 修改键值对的值 |

### 智能特性

- **自动识别最近的文本对象**：不需要光标精确定位
- **支持嵌套**：在多层嵌套中智能选择
- **多种引号类型**：自动识别单引号、双引号、反引号

## 🔥 常用操作

### 1. 修改函数参数

```javascript
function example(foo, bar, baz) {
  //           ^ 光标在这里，使用 cia
}

// 输入 cia -> 修改 'foo' 参数
// 输入 daa -> 删除 'foo,' （包括逗号）
```

### 2. 操作引号内容

```python
message = "Hello, World!"
#          ^ 光标在任意位置，使用 ciq
# ciq -> 修改引号内容
# daq -> 删除整个字符串（包括引号）
```

### 3. 处理嵌套结构

```javascript
const obj = { name: "John", age: 30 }
//                          ^ 光标在这里
# cik -> 修改 'age' 的值 (30)
# dak -> 删除整个键值对 'age: 30,'
```

### 4. HTML/JSX 标签操作

```jsx
<div className="container">
  <h1>Title</h1>
</div>

// 光标在 h1 内，使用 dit -> 删除 'Title'
// 使用 dat -> 删除整个 <h1>Title</h1>
```

## ⌨️ 常用快捷键

### 文本对象选择

```vim
vi<对象>  " 选择内部（inside）
va<对象>  " 选择周围（around，包含分隔符）
```

### 结合操作符

```vim
ci<对象>  " 修改内部
ca<对象>  " 修改周围
di<对象>  " 删除内部
da<对象>  " 删除周围
yi<对象>  " 复制内部
ya<对象>  " 复制周围
```

## 💡 实用技巧

### 1. 快速修改函数参数

```javascript
function sum(a, b, c) {
  return a + b + c
}

// 在任意参数上：
// cia  -> 修改当前参数
// daa  -> 删除当前参数（包括逗号）
// via  -> 选择当前参数（用于复制或其他操作）
```

### 2. 智能引号操作

```javascript
const str = 'single'
const str2 = "double"
const str3 = `template ${var}`

// 在任何引号内容上使用 ciq 都能正确识别引号类型
```

### 3. 处理对象/数组

```javascript
const config = {
  name: "app",
  version: "1.0.0",
  dependencies: ["react", "vue"]
}

// 光标在 "react" 上
// vi[  -> 选择数组所有元素
// vak  -> 选择整个 dependencies 键值对
```

## 🎨 高级用法

### 结合 `.` 重复操作

```javascript
function example(foo, bar, baz, qux) {
  // 光标在 foo 上
  // ciafirst<Esc>  -> 将 foo 改为 first
  // w.              -> 跳到 bar，重复操作（改为 first）
  // w.              -> 跳到 baz，继续重复
}
```

### 结合宏录制

```javascript
// 批量修改所有参数
// qa      -> 开始录制宏 a
// ciaNEW  -> 修改参数为 NEW
// w       -> 跳到下一个参数
// q       -> 结束录制
// @@      -> 重复执行宏
```

## 📚 参考

- `ia` / `aa` 是 mini-ai 最常用的功能（函数参数）
- 结合 `c`（修改）、`d`（删除）、`y`（复制）、`v`（选择）使用
- 不需要精确定位光标，插件会自动找到最近的文本对象

## 🔗 相关资源

- [GitHub - mini.ai](https://github.com/echasnovski/mini.nvim/blob/main/readmes/mini-ai.md)
- [AstroCommunity 插件页](https://github.com/AstroNvim/astrocommunity/tree/main/lua/astrocommunity/motion/mini-ai)
