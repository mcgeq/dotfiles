return {
  -- Enable to auto-read the latest saved workspace session on empty startup.
  autoread = false,

  -- Save the active workspace session automatically on exit.
  autowrite = true,

  -- Optional custom session directory.
  -- directory = vim.fn.stdpath("data") .. "/session",
}
