# User Ftplugin

If you want per-filetype overrides without touching the base config, add files under `after/ftplugin/` in your local branch, or keep helper logic in `lua/user/autocmds.lua`.

Examples:

- `after/ftplugin/markdown.lua`
- `after/ftplugin/rust.lua`
- `after/ftplugin/sql.lua`
