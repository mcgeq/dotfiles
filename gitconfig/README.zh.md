# Git 配置

全局 `.gitconfig`，包含用户信息、别名和差异设置。

## 文件

- `gitconfig` — 全局 git 配置文件

## 内容

- 用户名和邮箱
- 换行符和编码设置
- Git 别名（快捷命令）
- Delta 差异查看器配置（如已配置）
- 凭据管理器

## 备注

- `hooks` 和 `commit.txt` 已废弃 — 请使用 `commit/`（husky + commitlint）。

详见 [gitconfig](gitconfig)。
