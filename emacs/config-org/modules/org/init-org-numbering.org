#+TITLE: Org Numbering Module
#+AUTHOR: mcge
#+PROPERTY: header-args:emacs-lisp :tangle init-org-numbering.el :lexical t

* Org Numbering Module

Org Numbering - numbering mode for org.

** File Header

#+begin_src emacs-lisp
;;; init-org-numbering.el --- Org Numbering for MCG Emacs -*- lexical-binding: t; -*-

;; Copyright (C) 2024-2026 MCG
;; Author: mcge <mcgeq@outlook.com>
;; Keywords: org, org-numbering
#+end_src

** Require

#+begin_src emacs-lisp
;;; Code:

(require 'core-lib)
#+end_src

** Configuration

#+begin_src emacs-lisp
;;; ============================================================
;;; Configuration
;;; ============================================================

(defun mcg--setup-org-numbering ()
  "Set up org-numbering."
  (when (mcg-require-extension "org-numbering" t)
    (mcg-log "Org numbering configured")))
#+end_src

** Initialize

#+begin_src emacs-lisp
(mcg--setup-org-numbering)
#+end_src

** Provide

#+begin_src emacs-lisp
(provide 'init-org-numbering)
;;; init-org-numbering.el ends here
#+end_src
