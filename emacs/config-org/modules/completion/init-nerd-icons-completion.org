#+TITLE: Nerd Icons Completion Module
#+AUTHOR: mcge
#+PROPERTY: header-args:emacs-lisp :tangle init-nerd-icons-completion.el :lexical t

* Nerd Icons Completion Module

配置 nerd-icons-completion，在补全列表中显示图标。

** File Header

#+begin_src emacs-lisp
;;; init-nerd-icons-completion.el --- nerd-icons-completion -*- lexical-binding: t; -*-

;;; Commentary:
;; Configure nerd-icons-completion from:
;; emacs/site-lisp/extensions/ui/themes/nerd-icons-completion

;;; Code:

(require 'core-lib)
#+end_src

** Setup

#+begin_src emacs-lisp
(defun mcg--setup-nerd-icons-completion ()
  "Setup nerd-icons-completion."
  (when (and (mcg-require-extension "nerd-icons" t)
             (mcg-require-extension "nerd-icons-completion" t))
    (nerd-icons-completion-mode 1)
    (with-eval-after-load 'marginalia
      (add-hook 'marginalia-mode-hook #'nerd-icons-completion-marginalia-setup))
    (mcg-log "nerd-icons-completion configured")))
#+end_src

** Initialize

#+begin_src emacs-lisp
(mcg--setup-nerd-icons-completion)
#+end_src

** Provide

#+begin_src emacs-lisp
(provide 'init-nerd-icons-completion)
;;; init-nerd-icons-completion.el ends here
#+end_src
