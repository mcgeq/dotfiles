#+TITLE: MCG Emacs Private Configuration
#+AUTHOR: Your Name
#+PROPERTY: header-args:emacs-lisp :tangle config.el :lexical t

* Private Configuration
:PROPERTIES:
:HEADER-ARGS: :tangle config.el :lexical t
:END:

** Headers
#+BEGIN_SRC emacs-lisp
  ;;; config.el --- Private Configuration -*- lexical-binding: t; -*-

  ;; Filename: config.el
  ;; Description: User private configuration
  ;; Author: Your Name
  ;; Copyright (C) 2024-2026, Your Name, all rights reserved.
  ;; Create   Date: 2026-01-07
  ;; Version: 1.0
  ;; Keywords: private, configuration
  ;; Compatibility: GNU Emacs 29.0+

  ;;; Commentary:
  ;;
  ;; This file is for user private configuration, not tracked by Git.
  ;;
  ;; You can configure:
  ;; - Personal information (username, email)
  ;; - Custom paths
  ;; - Private keybindings
  ;; - Work-related configuration
  ;; - Other personal preferences
  ;;

#+END_SRC

** User Information
#+BEGIN_SRC emacs-lisp
;;; User Information

;; Set username and email
(setq user-full-name "Your Name"
      user-mail-address "your.email@example.com")

#+END_SRC

** Custom Paths
#+BEGIN_SRC emacs-lisp
;;; Custom Paths

;; Example: Set org files directory
;; (setq org-directory "~/Documents/org")

;; Example: Set notes directory
;; (setq mcg-notes-dir "~/Documents/notes")

;; Example: Set projects directory
;; (setq mcg-projects-dir "~/Projects")

#+END_SRC

** Theme Customization
#+BEGIN_SRC emacs-lisp
;;; Theme Customization

;; Example: Set default theme
;; (setq mcg-default-theme 'ef-maris-light)

;; Example: Set font size
;; (when mcg-is-gui
;;   (set-face-attribute 'default nil :height 140))

#+END_SRC

** Editor Preferences
#+BEGIN_SRC emacs-lisp
;;; Editor Preferences

;; Example: Set indentation width
;; (setq-default tab-width 4)
;; (setq-default indent-tabs-mode nil)

;; Example: Enable line numbers
;; (global-display-line-numbers-mode 1)

;; Example: Set auto-save
;; (setq auto-save-default t)

#+END_SRC

** LSP Configuration
#+BEGIN_SRC emacs-lisp
;;; LSP Configuration

;; Example: Set Python LSP server
;; (setq mcg-python-lsp-server "python-ty-ruff")

;; Example: Disable format on save
;; (setq mcg-lsp-format-on-save nil)

#+END_SRC

** Org Mode Configuration
#+BEGIN_SRC emacs-lisp
;;; Org Mode Configuration

;; Example: Set agenda files
;; (setq org-agenda-files '("~/Documents/org/agenda"))

;; Example: Set capture templates
;; (setq org-capture-templates
;;       '(("t" "Todo" entry (file+headline "~/Documents/org/todo.org" "Tasks")
;;          "* TODO %?\n  %i\n  %a")
;;         ("n" "Note" entry (file+headline "~/Documents/org/notes.org" "Notes")
;;          "* %?\n  %i\n  %a")))

#+END_SRC

** Custom Keybindings
#+BEGIN_SRC emacs-lisp
;;; Custom Keybindings

;; Example: Global keybindings
;; (global-set-key (kbd "C-c o") #'org-capture)
;; (global-set-key (kbd "C-c a") #'org-agenda)

;; Example: Use mcg-after! to set keybindings after module loads
;; (mcg-after! magit
;;   (global-set-key (kbd "C-c g") #'magit-status))

#+END_SRC

** Work-specific Configuration
#+BEGIN_SRC emacs-lisp
;;; Work-specific Configuration

;; Example: Set proxy
;; (setq url-proxy-services
;;       '(("http" . "proxy.example.com:8080")
;;         ("https" . "proxy.example.com:8080")))

;; Example: Set company-specific code style
;; (mcg-after! (:lang . typescript)
;;   (setq typescript-indent-level 2))

#+END_SRC

** Debug Settings
#+BEGIN_SRC emacs-lisp
;;; Debug Settings

;; Enable debug mode (show verbose logs)
;; (setq mcg-debug-mode t)

;; Enable Emacs debugging
;; (setq debug-on-error t)

#+END_SRC

** Provide
#+BEGIN_SRC emacs-lisp
(provide 'config)
;;; config.el ends here
#+END_SRC
